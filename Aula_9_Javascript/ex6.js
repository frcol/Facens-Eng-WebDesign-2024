// Função que mostra o dobro de um número
function dobro(numero) {
    if (numero > 0) {
        console.log("O dobro de " + numero + " é " + (numero * 2));
    } else {
        console.log("Só é aceito números positivos maiores que zero");
    }
}

dobro(4);  // Exemplo de chamada
